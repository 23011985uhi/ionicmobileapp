import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uhi.23011985',
  appName: 'ionicmobileapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;

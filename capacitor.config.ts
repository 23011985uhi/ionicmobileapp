import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ionicmobileapp',
  appName: 'ionicmobileapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
